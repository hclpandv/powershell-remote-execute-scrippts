# powershell-remote-execute-scrippts

Collection of usable PS scripts which can be executed remotly from this repo itself.

*pre-requisites : You Must have admin privilidges on machine where you are executing

Example:

```
```
